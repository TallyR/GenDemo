
import Navbar from "@/app/ui/Navbar";

export default function ProspectView() {
    return (
        <div className="min-w-full">
            <Navbar url="Prospecting / Approov Security" button={{title: "Export"}} />
        </div>
    );
}
