import { Libre_Caslon_Display } from "next/font/google"; 
import { Libre_Caslon_Text } from "next/font/google"; 

export const libre_caslon_display = Libre_Caslon_Display({subsets: ['latin'], weight: ['400']});
export const libre_caslon_display_bold = Libre_Caslon_Text({subsets: ['latin'], weight: ['700'], style: ['normal']});
export const libre_caslon_display_thin = Libre_Caslon_Text({subsets: ['latin'], weight: ['400'], style: ['normal']});